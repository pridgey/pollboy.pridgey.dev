import { Background } from "./../Background";
import { StyledLoader } from "./Loader.styles";

export const Loader = () => (
  <StyledLoader>
    <Background>
      <p>Pollboy</p>
    </Background>
  </StyledLoader>
);

import styles from "./PollResults.module.css";

export type PollResultsProps = {
  PollID: number;
};

export const PollResults = (props: PollResultsProps) => {
  return <div>Results Table Goes Here</div>;
};
